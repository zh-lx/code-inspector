import { defineComponent, onMounted, ref } from 'vue';

export default defineComponent({
  setup(props, { emit }) {
    onMounted(() => {
      // ...
    });
    return () => (
      <div>
        detail jsx paragraph
        <p>sfaasfa</p>
        <a>sagasghiqlhj</a>
      </div>
    );
  },
});
